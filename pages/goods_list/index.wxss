.goods_list{
    display: flex;
    height: 90rpx;
}
.goods_tab{
    flex: 1;
    flex-direction: column;
    text-align: center;
    
    line-height: 90rpx;
}
.active{
    color: var(--themeColor);
    border-bottom: 1rpx solid red;
}
